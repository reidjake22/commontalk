from typing import List, Dict

def save_clusters(clusters: List[Dict]) -> tuple:
    """Save clusters to database and return cluster_group_id and cluster_ids"""
    # TODO: Implement database saving
    # Should return (cluster_group_id, list_of_cluster_ids)
    return (12345, [1, 2, 3, 4, 5])
