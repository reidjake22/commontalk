# Here's the config file, a lot of Nones to tell you the structure
default_config = {
    "max_depth": 2,
    "min_points": 3,
    "method": "kmeans",
    "n_clusters": 3,  # ADD this for KMeans
    "skip_llm": False,  # ADD this for LLM control
}